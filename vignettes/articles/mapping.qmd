---
title: "Theme mapping"
subtitle: Map theme elements between formats
description: Map theme elements between formats
tbl-cap-location: bottom
knitr:
  opts_chunk:
    collapse: true
    comment: '#>'
---

This article summarizes how **rstudiothemes** maps theme elements between
**Visual Studio Code**, **Positron**, **TextMate** and **RStudio** formats.

Sources:

- **TextMate** and **Visual Studio Code**:
  [vscode-generator-code](https://github.com/microsoft/vscode-generator-code/blob/main/generators/app/generate-colortheme.js)
- **TextMate** and **RStudio**:
  <https://rstudio.github.io/rstudio-extensions/rstudio-theme-creation.html>

When several source elements map to the same target element, the mapping
priority determines which value is used.

::: {#tbl-map}
```{r}
#| echo: false
tb <- read.csv(system.file("csv/mapping.csv", package = "rstudiothemes"))

knitr::kable(tb)
```

Mapping between theme elements and formats.
:::
